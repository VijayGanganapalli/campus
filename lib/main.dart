import 'package:flutter/material.dart';

void main() => runApp(CampusApp());

class CampusApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Center(
        child: Text('Hey there!'),
      ),
    );
  }
}
